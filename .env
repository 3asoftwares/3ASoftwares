# This file is tracked in git — never put real secrets here.
# Real values (MONGODB_URI, RAZORPAY_*) belong in .env.local, which is gitignored
# and takes priority over this file.
#
# See .env.local.example (local dev) and .env.production.example (production
# deployment) for the exact variables each environment needs.
